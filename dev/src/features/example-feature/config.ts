/**
 * Example Feature - Configuration
 *
 * Define all constants, tunable values, and configuration data here.
 * This makes it easy to tweak behavior without modifying logic code.
 */

/**
 * How often the think loop executes (in seconds)
 */
export const THINK_INTERVAL = 2.0;

/**
 * Prefix for all console messages from this feature
 */
export const MESSAGE_PREFIX = "[ExampleFeature]";

/**
 * Maximum number of messages to log before pausing
 */
export const MAX_MESSAGES = 10;

/**
 * Enable verbose debug logging
 */
export const DEBUG_MODE = true;
